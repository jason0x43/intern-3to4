{
	"suites": [
		"_build/tests/unit/**/*.js"
	],
	"plugins": "_build/tests/support/MockNodeModule.js",

	"environments": [
		{ "browser": "node" }
	],

	"configs": {
		"coverage": {
			"description": "Generate full coverage",
			"coverage": [
				"_build/src/**/*.js"
			],
			"reporters+": [ "htmlcoverage" ]
		}
	}
}
